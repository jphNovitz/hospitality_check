{% extends 'base.html.twig' %}

{% block title %}Edit Base{% endblock %}

{% block body %}
    <h1>Edit Base</h1>

    {{ include('preference/base/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_preference_base_index') }}">back to list</a>

    {{ include('preference/base/_delete_form.html.twig') }}
{% endblock %}
