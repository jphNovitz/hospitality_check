<div  class="w-full flex flex-col">
    <twig:Title:H2>{% trans %} title.resident.basic{% endtrans %}</twig:Title:H2>
    {% if resident.bases  is not empty %}
        <div class="w-full grid md:grid-cols-2 lg:grid-cols-3 p-1 my-4 gap-4">
            {% for base in resident.bases %}
                <twig:BaseItem :base="base" />
               {# <div class="w-full flex">
                    <div class="w-1/12 aspect square p-1">
                        {% if base.icon is not empty %}
                            <img alt="{{ base.name }}"
                                 src="{{ asset('images/base/'~base.icon) }}"
                                 class="w-full aspect-square"/>
                        {% else %}
                            <div class="w-full aspect-square"> NO IMAGE</div>
                        {% endif %}
                    </div>
                    <div class="w-11/12 inline-flex items-center font-semibold text-xl p-1">
                        {{ base.name }}
                    </div>
                </div>#}
            {% endfor %}
        </div>
    {% else %}
        <div class="p-6">
            Pas de préférences de bases ?!
        </div>
    {% endif %}
</div>
