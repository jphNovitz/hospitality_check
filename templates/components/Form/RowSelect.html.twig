<div {{ attributes.defaults({ class: 'flex flex-col my-8' }) }}>
    {{ form_label(row,null, {
        'label': label,
        'label_attr': {'class': 'font-semibold'}
    }) }}
    {{ form_widget(row, { 'attr':
        {
            'class': 'w-full bg-gray-50 rounded-md px-1 py-2 mt-3 focus:outline-none border-black-200 focus:ring focus:border-black-900'
        }
    }) }}

</div>