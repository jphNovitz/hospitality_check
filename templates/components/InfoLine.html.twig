<div {{ attributes.defaults({ class: "w-full flex flex-col"}) }}>
    <div class="font-italic font-weight-light text-sm">
        {% if label is defined %}
            {{ label }}
        {% endif %}
    </div>
    <div class="py-1 font-semibold text-xl">
        {% block content %} {% endblock %}
    </div>
</div>