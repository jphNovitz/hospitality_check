<form method="post" action="{{ path('admin_resident_delete', {'id': resident.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resident.id) }}">
    <twig:Button:Submit href="{{ path('admin_resident_index') }}">
        {{ ux_icon('mdi:delete-outline', {'class': 'w-5 h-5'}) }} {% trans %}button.delete{% endtrans%}
    </twig:Button:Submit>
</form>
